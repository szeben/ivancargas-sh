<odoo>
  <data>

    <record model="ir.ui.view" id="view_tax_form_inherited">
      <field name="name">account.tax.form.inherited</field>
      <field name="model">account.tax</field>
      <field name="inherit_id" ref="account.view_tax_form" />
      <field name="arch" type="xml">

        <xpath expr="//group/label[@for='amount']" position="attributes">
          <attribute name="attrs">{'invisible':['|', ('amount_type', 'not in', ('fixed', 'percent', 'division')), ('withholding_type', 'in', ('islr', 'iva'))]}</attribute>
        </xpath>
        <xpath expr="//group/div[1]" position="attributes">
          <attribute name="attrs">{'invisible':['|', ('amount_type', 'not in', ('fixed', 'percent', 'division')), ('withholding_type', 'in', ('islr', 'iva'))]}</attribute>
        </xpath>
        <xpath expr="//group/field[@name='tax_scope']" position="after">
          <field name="withholding_type" attrs="{'invisible':[('amount_type', 'not in', ('fixed', 'percent', 'division'))]}" />
          <label for="withholding_amount" attrs="{'invisible':['|', ('amount_type', 'not in', ('fixed', 'percent', 'division')), ('withholding_type', 'not in', ('islr', 'iva'))]}"/>
          <div attrs="{'invisible':['|', ('amount_type', 'not in', ('fixed', 'percent', 'division')), ('withholding_type', 'not in', ('islr', 'iva'))]}">
            <field name="withholding_amount" class="oe_inline" nolabel="1"/>
            <span class="o_form_label oe_inline" attrs="{'invisible':[('amount_type','=','fixed')]}">%</span>
          </div>
        </xpath>

      </field>
    </record>

    <record id="view_move_form_inherit" model="ir.ui.view">
      <field name="name">account.move.form.inherit</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_move_form" />
      <field name="arch" type="xml">

        <field name="invoice_vendor_bill_id" position="after">
          <field name="invoice_tax_id" />
        </field>

      </field>
    </record>

    <record id="view_invoice_tree_inherit" model="ir.ui.view">
      <field name="name">account.invoice.tree.inherit</field>
      <field name="model">account.move</field>
      <field name="inherit_id" ref="account.view_invoice_tree" />
      <field name="arch" type="xml">

        <field name="ref" position="after">
          <field name="withholding_iva" optional="show"/>
          <field name="withholding_islr" optional="show"/>
        </field>

      </field>
    </record>

  </data>
</odoo>